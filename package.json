{
  "name": "calcaxy.com",
  "private": true,
  "version": "1.0.0",
  "description": "calcaxy",
  "scripts": {
    "server": "bundle exec middleman server",
    "build": "bundle exec middleman build --verbose",
    "docker:bash": "docker-compose run --rm bash",
    "docker:build": "docker-compose run --rm build",
    "docker:server": "docker-compose run --rm --service-ports server"
  },
  "license": "Creative Commons 4.0",
  "homepage": "https://calcaxy.com"
}
